{
  "name": "hd2-strategist",
  "version": "0.1.0",
  "private": true,
  "description": "Headless Galactic War MCP server on Cloudflare Workers — a correctness layer over api.helldivers2.dev",
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "test": "vitest run",
    "typecheck": "tsc --noEmit"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20250601.0",
    "typescript": "^5.5.0",
    "vitest": "^2.1.0",
    "wrangler": "^4.0.0"
  }
}
